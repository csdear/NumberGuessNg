NumberGuessNg.
The classic bulls and cows game.  Bull = correct position.  Cow = correct number


|TAGS|
signals,input, inputChange, onCheckboxChange,service,eventService,tailwind,daisyui,page,hero,mouseEvent,event.target.value,ngModel,checkboxes,flex-centering,validation,length must be exact,must be unique(!33), signals update, signals set, signals update set, signals update history with an array, computed, conditionalRender, CR,[checked], (click), ngIf, @for, anchor, (change), Set, Set add, Set has, includes, .map split, ts for, return destructure,       